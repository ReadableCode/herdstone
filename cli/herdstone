#!/usr/bin/env bash
# Thin wrapper — delegates to the Python engine via uv
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_DIR="$SCRIPT_DIR/.."

exec uv run --project "$PROJECT_DIR" herdstone "$@"

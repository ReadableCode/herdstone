#!/usr/bin/env bash
# Thin wrapper — delegates to the Python engine via uv
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
ENGINE_DIR="$SCRIPT_DIR/../engine"

exec uv run --project "$ENGINE_DIR" herdstone "$@"

# Herdstone secrets — the real .env here is a gitignored symlink to
# ../personal_credentials/personal.env. Herdstone itself currently needs no
# API keys: the media remote (and its Sonarr/Radarr/Plex keys) moved to the
# Sync_Plex repo. The symlink convention stays so future keys have a home —
# add placeholders here (never real values) when the engine grows one.
